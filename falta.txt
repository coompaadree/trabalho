dúvidas:
433 - meter as duas "out of the network" ou só uma?



falta:

 - limpar/rever código (mudar nome das variáveis finais, etc...)

 - fazer especificações

 - comentarios nas funções DFS e testSP

 - ordenação do ficheiro output em caso de empates
"Para cada pedido, as linhas dos respetivos três percursos são ordenados de cima para baixo por ordem crescente de duração. Em caso de empate no tempo, do que passa por mais estações para o que passa por menos estações. Em caso de empate ainda, em termos de número de estações, por ordem lexicográfica dos nomes da segunda estação no percurso."

- construir mais dois test set:
* um só com a exceção de "X and Y do not communicate"
* um só com a exceção de "X out of the network"

Já temos um test set sem exceções (test1) e um test set com as duas exceções (test2)

- construir um test set que só dê 2 caminhos possíveis, num dos casos (para ver se está corretamente ordenado)
